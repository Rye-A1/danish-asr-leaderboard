# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Eval artifacts
eval_audio_cache/
*.manifest.jsonl
results_rescored*/

# Cached parquet pulled from HF by push_results.py / update_space.py
data/

# Model / data blobs
*.nemo
*.wav
# Compiled outputs parquets are generated by CI and pushed to HF, never committed.
# Submissions carry the human-readable outputs/<model>.jsonl instead.
*.parquet

# Logs
*.log
*.err
*.out

# OS / editor / tooling
.DS_Store
.idea/
.vscode/
.claude/

# AppMap instrumentation 
appmap.yml
space/local-preview.html
uv.lock

# Env
*.env

# Local secrets (Mattermost webhook, etc.)
.env

# tmp files
tmp/
**/tmp/
tmp.md

# Generated by scripts/update_space.py at deploy time and uploaded from there.
# Keeping it tracked meant the committed copy rotted between deploys, and it
# conflicted on every branch that regenerated it.
space/leaderboard.json
