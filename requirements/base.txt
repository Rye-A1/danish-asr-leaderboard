# Core dependencies shared by every backend.
# Backend-specific files (-r base.txt) add model frameworks on top.
#
# System deps (install separately):
#   apt install ffmpeg
#
datasets>=2.19.0
huggingface_hub>=0.23.0
jiwer>=3.0.4
pandas>=2.0.0
pyarrow>=14.0.0
soundfile>=0.12.1
