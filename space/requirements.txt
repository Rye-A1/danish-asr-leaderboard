# Hugging Face Space (Gradio) — runtime dependencies.
# gradio_leaderboard requires gradio<6, so 5.50.0 is the newest usable gradio
# (keep this in sync with sdk_version in README.md).
gradio==5.50.0
gradio_leaderboard==0.0.14
pandas>=2.0.0
pyarrow>=14.0.0
huggingface_hub>=0.23.0
requests>=2.28.0
